apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: glorbo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: mixail.ermin33.fvds.ru:31339/glorbo-frontend:02
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
